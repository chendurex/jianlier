spring:
  application:
    name: jianli
  datasource:
    driverClassName: com.mysql.jdbc.Driver
    url: ${spring.data.url}
    username: ${spring.data.name}
    password: ${spring.data.password}

server:
  port: ${port:8080}

